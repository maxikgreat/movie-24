import moment from 'moment';import axios from '@services/axios';import { categories } from '@store/atoms';type FetchMoviesByCategoryResponse = {  page: number,  results: any[],  total_pages: number,  total_results: number,};export const fetchMoviesByCategory = (category?: typeof categories[number]) => {  console.log(category);  return axios.get<FetchMoviesByCategoryResponse>('/movie/popular?page=1')    .then(({ data }) => ({      ...data,      results: data.results.map((movie) => ({        ...movie,        release_date: moment(movie.release_date, 'YYYY-MM-DD')      }))    }));};