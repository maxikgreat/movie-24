import { atom } from 'recoil';import { Moment } from 'moment';type Genre = {  id: number,  name: string,};export type CurrentItem = {  id: number,  backdropPath: string,  releaseDate: Moment | null,  firstAirDate: Moment | null  genres: Genre [],  inProduction: boolean,  name: string,  overview: string,  posterPath: string,  tagline: string | null,  voteAverage: number,  createdBy: string | null,  budget: number | null,};export type CurrentMovieState = {  loading: boolean,  item: CurrentItem | null,  error: string | null,};export const applyDefaultValuesCurrentMovies = (): CurrentMovieState => ({  loading: false,  item: null,  error: null});// generic doesn't work here, i don't know whyexport const currentMovieState = atom({  key: 'currentMovieState',  default: applyDefaultValuesCurrentMovies() as CurrentMovieState});