import { selectorFamily, selector } from 'recoil';import { categories, moviesState, MovieCategory } from './atoms';export const categoriesList = selector({  key: 'categoriesList',  get: ({ get }) => Object.keys(get(moviesState))});export const categoryObject = selectorFamily<MovieCategory, typeof categories[number]>({  key: 'categoryObject',  get: (category) => ({ get }) => get(moviesState)[category]});