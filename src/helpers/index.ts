import { categories as storeCategories } from '@store/atoms';export const renameCategoryName = (category: typeof storeCategories[number]): string => {  switch (category) {  // case 'movie':  //   return 'Popular movies';  case 'tv':    return 'Popular TV\'s';  default:    return '';  }};export const shortText = (text: string, value: number): string | '' => {  if (!text) return '';  return text.length > value ? `${text.substring(0, value - 3)}...` : text;};export const applyColor = (vote: number) => {  if (vote > 1 && vote < 3.33) {    return 'text-red-400';  }  if (vote > 3.33 && vote < 6.66) {    return 'text-yellow-400';  }  return 'text-green-400';};