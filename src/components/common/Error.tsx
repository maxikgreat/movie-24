import React, { VFC } from 'react';import { Error as ErrorIcon } from '@components/icons';interface ErrorProps {  message?: string,  tryAgain?: Function,}const Error: VFC<ErrorProps> = ({ message, tryAgain }) => {  return (    <div className="h-52 flex flex-col items-center justify-center">      <ErrorIcon className="text-red-400 mr-1" />      {message && (        <span className="block my-2">{message}</span>      )}      {tryAgain && (        <button          className="transition-all bg-yellow-400 rounded-md shadow-md hover:shadow-lg px-4 py-1 text-white uppercase text-sm"          onClick={() => tryAgain()}        >Try again!</button>      )}    </div>  );};export default Error;