import React, { VFC } from 'react';import { CurrentItem } from '@store/atoms';import { CurrentItemDetailsFooter, CurrentItemDetailsHeader } from '@components/pages/CurrentItem';interface CurrentItemDetailsProps {  item: CurrentItem};const CurrentItemDetails: VFC<CurrentItemDetailsProps> = ({ item }) => {  return (    <div className="px-5 py-10 md:grid md:grid-flow-col md:grid-cols-3">      <div className="flex justify-center items-start mb-4 md:mb-0">        <img          src={`http://image.tmdb.org/t/p/w342${item.posterPath}`}          className="shadow-md object-cover w-100"          alt={item.name}        />      </div>      <div className="md:col-span-2 px-4 flex flex-col justify-between">        <CurrentItemDetailsHeader          name={item.name}          date={item.releaseDate ?? item.firstAirDate}          tagline={item.tagline}        />        <hr />        <p className="text-lg my-4 md:my-0">{item.overview}</p>        <hr />        <CurrentItemDetailsFooter          voteAverage={item.voteAverage}          isProduction={item.inProduction}          createdBy={item.createdBy}        />      </div>    </div>  );};export default CurrentItemDetails;