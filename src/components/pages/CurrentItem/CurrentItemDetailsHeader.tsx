import React, { VFC } from 'react';import { Moment } from 'moment';interface CurrentItemDetailsHeaderProps {  name: string,  date: Moment | null,  tagline: string | null,}const CurrentItemDetailsHeader:VFC<CurrentItemDetailsHeaderProps> = ({ name, date, tagline }) => (  <div className="mb-4 md:mb-0">    <div className="flex flex-col sm:flex-row justify-between items-start md:items-end">      <h1 className="text-3xl sm:text-4xl lg:text-5xl xl:text-6xl whitespace-nowrap truncate">{name}</h1>      {date && (        <span className="inline-block mb-1 uppercase text-sm lg:text-md xl:text-xl whitespace-nowrap">first appeared:            &nbsp;{date.format('DD MMM YYYY')}        </span>      )}    </div>    {tagline && <span className="italic text-md sm:text-lg">{tagline}</span>}  </div>);export default CurrentItemDetailsHeader;