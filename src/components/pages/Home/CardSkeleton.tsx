import { VFC } from 'react';import ContentLoader from 'react-content-loader';import { randomizeWidth } from '@helpers/index';const LINE_POINTS = [250, 280, 320, 350];const CardSkeleton: VFC = () => {  return (    <ContentLoader viewBox="0 0 300 420" height={420} width={300}>      <rect x="3" y="3" rx="10" ry="10" width="80%" height="180" />      {LINE_POINTS.map((y) => (        <rect          key={y}          x="4"          y={y}          rx="0"          ry="0"          width={randomizeWidth(50, 80)}          height="20"        />      ))}    </ContentLoader>  );};export default CardSkeleton;