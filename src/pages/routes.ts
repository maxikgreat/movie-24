import { ComponentType } from 'react';import loadable from '@loadable/component';// import Home from '@pages/Home';const Home = loadable(() => import(/* webpackChunkName: "home" */'./Home'));const CurrentItem = loadable(() => import(/* webpackChunkName: "current-item" */'./CurrentItem'));interface Route {  name: string,  component: ComponentType,  path: string,  exact: boolean,};const routes: Route[] = [  {    name: 'Home',    component: Home,    path: '/',    exact: true  },  {    name: 'CurrentItem',    component: CurrentItem,    path: '/:category/:id',    exact: false  }];export default routes;