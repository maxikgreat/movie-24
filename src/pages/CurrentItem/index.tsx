import React, { useEffect, VFC } from 'react';import { useParams, Redirect } from 'react-router-dom';import { categories as storeCategories, categories } from '@store/atoms';import { CurrentItemDetails, CurrentItemSkeleton } from '@components/pages/CurrentItem';import { Error } from '@components/common';import { useCurrentItem } from '@hooks/index';const CurrentItem: VFC = () => {  const { category, id } = useParams<{    category: typeof storeCategories[number],    id: string  }>();  if (!categories.includes(category as typeof storeCategories[number])) {    return <Redirect to="/" />;  }  const { currentItem, fetchItemByIdHandler, clearItem } = useCurrentItem();  useEffect(() => {    fetchItemByIdHandler(category, id);    return () => clearItem();  }, []);  return (    <section>      {(currentItem.item && !currentItem.loading && !currentItem.error) && (        <CurrentItemDetails          item={currentItem.item}        />      )}      {(currentItem.loading && !currentItem.error) && (        <CurrentItemSkeleton />      )}      {(!currentItem.loading && currentItem.error) && (        <Error message={currentItem.error} tryAgain={() => fetchItemByIdHandler(category, id)} />      )}    </section>  );};export default CurrentItem;