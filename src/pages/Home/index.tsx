import React, { VFC } from 'react';import Animated from 'react-mount-animation';import { MoviesCarousel, Search } from '@components/pages/Home';import { categories as storeCategories } from '@store/atoms';import { searchConfig } from '@configs/index';import { useMovies, useSearch } from '@hooks/index';const Home: VFC = () => {  const { categories, fetchMoviesHandler } = useMovies();  const { search, searchMovieHandler } = useSearch();  return (    <section className="py-5">      <Animated.div        show={search.searchValue !== ''}        mountAnim={searchConfig.mountSearchCss}        unmountAnim={searchConfig.unmountSearchCss}      >        <Search          search={search}          searchMovieHandler={searchMovieHandler}        />      </Animated.div>      {categories.map((category, index) => (        <MoviesCarousel          key={category}          name={category as typeof storeCategories[number]}          index={index}          fetchHandler={(category: typeof storeCategories[number]) =>            fetchMoviesHandler(category)          }        />      ))}    </section>  );};export default Home;